# Replicate-3 programmatic reclassification

Replicate-3 is permanently classified as a **Programmatic Source-Constrained Composition Stress Test**.
It is not a Codex GPT-5.6 generation, an LLM-authored baseline, a GPT-4o reproduction, or an official
SmartGen generation reproduction.

The historical builder selected actions by deterministic modular indexing over source-group vocabularies,
inserted observed source transitions, and rejected source/replicate-2 collisions before serialization. The
frozen reconstruction-health failure remains unchanged: 137 generated sequences passed the two generation
gates, TOF retained 135, and `reconstruction_health_v1` failed before any target behavior was read.

The immutable boundary is tag `replicate3-programmatic-reconstruction-failure`, pointing to commit
`cadef561de1a28facc4a05f7f24e4b6a8c8ee1b0`. No file under the frozen replicate-3 output directory is
modified by this reclassification.
